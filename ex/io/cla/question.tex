Write a program that takes two parameters from the command line, converts them to integers, adds them together, and prints the result to the screen. The two arguments come from the parameter to the \methodname{Main} method, which is usually called \varname{args}.
