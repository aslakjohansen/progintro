\inputminted[fontsize=\normalsize]{elixir}{\context/answer/ElemEq.ex}

Using higher-order functions it is a bit easier:

\inputminted[fontsize=\normalsize]{elixir}{\context/answer/ElemEqHO.ex}

But why does this work?
