Elixir has an \modulename{Enum} module that offers, among other things, a \funcname{map} and a \funcname{filter} function. In this exercise, you will write these functions from scratch in a module that you call \modulename{HigherOrder}.
