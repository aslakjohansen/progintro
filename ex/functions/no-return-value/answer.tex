A functions return type defines the type of the value that it returns. A special type called \typename{void} is used to indicate that no value is returned. When no value is returned, there is no need for a \methodname{return}-statement, and if present it doesn't take a parameter.
