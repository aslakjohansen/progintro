\begin{enumerate}
  \item Open a terminal, and go to the directory of the previous exercise.
  \item Using that terminal, create a subdirectory called \say{oop}.
  \item Still using the terminal, move the file from the previous exercise into the \say{oop} directory.
  \item Use a file namager to verify that you have accomplished the task.
\end{enumerate}
